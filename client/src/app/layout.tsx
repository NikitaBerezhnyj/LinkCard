import type { Metadata } from "next";
import {
  roboto,
  inter,
  openSans,
  shadowsIntoLight,
  raleway,
  montserrat,
  barrio,
  delius,
  audiowide,
  merriweather
} from "@/constants/fonts";
import "./globals.scss";
import { Toaster } from "react-hot-toast";
import ClientI18nextProvider from "@/providers/ClientI18nextProvider";

export const metadata: Metadata = {
  title: "LinkCard",
  description: "Generated by create next app"
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <head>
        <meta name="theme-color" content="#181818" />
      </head>
      <body
        className={`${roboto.variable} ${inter.variable} ${openSans.variable} ${shadowsIntoLight.variable} ${raleway.variable} ${montserrat.variable} ${barrio.variable} ${delius.variable} ${audiowide.variable} ${merriweather.variable}`}
      >
        <ClientI18nextProvider>{children}</ClientI18nextProvider>

        <Toaster
          position="top-right"
          toastOptions={{
            style: {
              background: "#181818",
              color: "#f3f4f6",
              border: "1px solid #2c2c2c"
            }
          }}
        />
      </body>
    </html>
  );
}
